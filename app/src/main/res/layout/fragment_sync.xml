<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_blue"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    tools:context=".SyncFragment">

    <TextView
        android:id="@+id/sync_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Synchronizing Data"
        android:textColor="@color/white"
        android:textSize="24sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="16dp"
        android:gravity="center" />

    <ProgressBar
        android:id="@+id/sync_progress_bar"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:indeterminateDrawable="@drawable/custom_progress_bar"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/sync_status_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Please wait while we sync your data..."
        android:textColor="@color/light_gray"
        android:textSize="16sp"
        android:gravity="center"
        android:fontFamily="sans-serif"
        android:layout_marginBottom="32dp" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/cancel_sync_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:textColor="@color/white"
        android:background="@drawable/rounded_btn_red"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:layout_gravity="center" />

</LinearLayout>